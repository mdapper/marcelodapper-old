import React from 'react';
import Container from '../Container/Container';
import Row from '../Row/Row';
import Section from '../Section/Section';

const NotFound = () => (
  <Section>
    <Container>
      <Row>
        <div>
          <h2>Page not found</h2>
          <p>This is the end,</p>
          <p>My only friend, the end!</p>
        </div>
      </Row>
    </Container>
  </Section>
);

export default NotFound;
